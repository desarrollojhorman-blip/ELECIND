# 📋 Resumen Fase 1 — Base de dominio y arranque del MVP

**Fecha:** 12/05/2026  
**Hora:** 15:00  
**Estado:** 🚧 EN CURSO

---

## 🎯 Qué se ha completado en Fase 1

- ✅ Consolidada la base de datos núcleo del dominio en una sola migración de catálogo.
- ✅ Creadas las entidades base para clientes, proyectos, materiales, lotes, movimientos y conceptos.
- ✅ Definidas relaciones Eloquent mínimas para poder construir los CRUD encima.
- ✅ Eliminadas migraciones duplicadas generadas al arrancar la fase.
- ✅ Validado el arranque del esquema con `migrate:fresh --seed`.
- ✅ Validado el estado del proyecto con tests, Larastan y Pint.

---

## 🧱 Dominio creado en esta fase

### Tablas base ya disponibles
- `empresas_clientes`
- `tipos_proyectos`
- `proyectos`
- `conceptos`
- `proyecto_usuario`
- `proyecto_concepto`
- `materiales`
- `material_proyecto`
- `material_lotes`
- `movimientos_stock`

### Relaciones principales ya preparadas
- `users.empresa_cliente_id` → empresa cliente asociada al usuario externo.
- `proyectos.empresa_cliente_id` → cada proyecto pertenece a una empresa cliente.
- `proyectos.tipo_proyecto_id` → tipo de proyecto opcional.
- `proyectos.responsable_principal_id` → usuario responsable del proyecto.
- Pivot `proyecto_usuario` → usuarios asignados a proyecto.
- Pivot `proyecto_concepto` → conceptos asignados a proyecto.
- Pivot `material_proyecto` → materiales previstos por proyecto.
- `material_lotes.material_id` → lotes por material.
- `movimientos_stock.material_lote_id` → trazabilidad de entradas, salidas y ajustes.

---

## 📁 Archivos clave actualizados

### Migración central
- `database/migrations/2026_05_12_120300_create_core_catalog_tables.php`

### Modelos preparados para seguir con CRUD
- `app/Models/EmpresasCliente.php`
- `app/Models/TiposProyecto.php`
- `app/Models/Proyecto.php`
- `app/Models/Material.php`
- `app/Models/MaterialLote.php`
- `app/Models/MovimientoStock.php`
- `app/Models/Concepto.php`
- `app/Models/User.php`

### Limpieza realizada
- Eliminadas las migraciones duplicadas creadas por `make:model -m` para evitar colisiones con la migración central.

---

## ✅ Validaciones ejecutadas

### Base de datos
```bash
C:\xampp\php\php.exe artisan migrate:fresh --seed
```

Resultado:
- Migraciones ejecutadas correctamente.
- Seeders base ejecutados correctamente.
- Usuarios `superadmin` y `admin` regenerados sin incidencias.

### Tests
```bash
C:\xampp\php\php.exe artisan test
```

Resultado:
- 2 tests OK.

### Análisis estático
```bash
C:\xampp\php\php.exe C:\ProgramData\ComposerSetup\bin\composer.phar analyse
```

Resultado:
- Sin errores de Larastan.

### Estilo
```bash
C:\xampp\php\php.exe C:\ProgramData\ComposerSetup\bin\composer.phar lint
```

Resultado:
- Pint pasando en todos los archivos actuales.

---

## 🔑 Decisiones tomadas en Fase 1

- Se mantiene una **migración central de catálogo** para esta primera base del dominio, evitando fragmentación temprana.
- Se respetan nombres de tabla del dominio en español: `materiales`, `movimientos_stock`, `empresas_clientes`, etc.
- `MaterialLote` se mantiene como pieza de trazabilidad de stock y `MovimientoStock` como libro de movimientos.
- Se dejan listas las relaciones mínimas antes de abrir los CRUD para evitar rehacer estructura después.

---

## 📌 Estado real del roadmap tras este avance

### Fase 0
- ✅ Completada y validada.

### Fase 1
- ✅ Modelos + migraciones base del dominio.
- ✅ Login + middleware ya disponibles desde Fase 0 y vigentes.
- ⏳ Pendiente crear factories y seeders de negocio.
- ⏳ Pendiente montar `data-table` reutilizable.
- ⏳ Pendiente empezar CRUD de `EmpresasClientes`.

### Fases 2 en adelante
- ⏸️ Aún no iniciadas.

---

## ▶️ Siguiente paso recomendado

El siguiente bloque lógico para continuar Fase 1 es:

1. Crear `factories` y `seeders` de `EmpresasCliente`, `TiposProyecto`, `Proyecto`, `Material`, `MaterialLote` y `Concepto`.
2. Arrancar el primer CRUD funcional con `EmpresasClientes`.
3. Después encadenar `TiposProyecto` y `Proyectos`.

---

## ⏳ Qué falta por hacer ahora

Para que Fase 1 avance de verdad hacia un MVP usable, todavía falta completar estos bloques:

1. **Datos de prueba del negocio**
   - Crear factories y seeders para poblar empresas, tipos de proyecto, proyectos, materiales, lotes y conceptos.
   - Dejar escenarios mínimos para desarrollo y pruebas manuales.

2. **Primer CRUD funcional**
   - Empezar por `EmpresasClientes`.
   - Alta, edición, borrado lógico y listado.
   - Validaciones de negocio básicas: nombre, CIF, contacto, estado activo.

3. **Infraestructura reutilizable de listados**
   - Construir el `data-table` Livewire reutilizable si queremos acelerar los siguientes CRUD.
   - Si no se hace ahora, el primer CRUD puede salir con tabla simple y extraer luego el componente.

4. **Continuación del dominio administrativo**
   - CRUD de `TiposProyecto`.
   - CRUD de `Proyectos` con asignaciones de usuarios, conceptos y materiales.
   - CRUD de `Materiales` y gestión de `MaterialLotes`.

5. **Capa de permisos avanzada**
   - Extender roles/permisos con `nivel`, `acceso` y `es_sistema` si eso sigue siendo requisito del roadmap.
   - Preparar el terreno para el CRUD de usuarios con filtros por nivel.

6. **Configuración de empresa**
   - Pantalla para logo, colores, datos de empresa y plantilla de numeración.

---

## ✅ Qué deberíamos hacer ahora mismo

La secuencia más razonable desde este punto es:

1. Crear factories y seeders de Fase 1.
2. Implementar el CRUD de `EmpresasClientes`.
3. Decidir si el `data-table` se monta antes del segundo CRUD o durante el primero.

---

## 📝 Resumen ejecutivo

Fase 1 no está terminada, pero ya tiene cerrada su base técnica: el dominio principal existe en base de datos, las relaciones entre entidades están modeladas y el proyecto sigue estable tras migrar, sembrar, analizar y testear. A partir de aquí ya se puede entrar en desarrollo de CRUD y UI sin volver a rehacer estructura.

---

## 📊 Métricas de estado actual

| Métrica | Valor |
|---------|-------|
| Fase 0 | Completada |
| Fase 1 | En curso |
| Migraciones activas | 7 |
| Nuevas tablas de dominio Fase 1 | 10 |
| Tests | 2 |
| Analyse | OK |
| Lint | OK |
| Estado documentación | Actualizada hasta el arranque de Fase 1 |

---

## 📝 Notas

- **Sesiones en BD** — Configuradas para manejo distribuido
- **Passwords** — Hasheadas con bcrypt (Laravel default)
- **Timezone** — Configurado a zona horaria del servidor
- **Activity Log** — Listo para auditoría en Fase 1+
- **MediaLibrary** — Listo para cargas de archivos en Fase 1+

---

**Fase 1 está arrancada y estabilizada a nivel técnico. El siguiente trabajo real debe centrarse en seeders y en el primer CRUD de negocio.**
