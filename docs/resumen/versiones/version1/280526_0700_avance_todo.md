# Resumen de avance — 28/05/2026

---

## 1. Filtros: número de empleado, código de cliente y código de proyecto

**Módulos afectados:** Horas, Ausencias, Incidencias

Los selects de filtro ahora muestran identificadores visuales delante del nombre:
- Trabajador → `001 · García López Juan`
- Cliente → `COD · Empresa SA`
- Proyecto → `(COD) Nombre del proyecto`

Se añadió la propiedad `filtrosVersion` con `wire:key` para forzar la reinicialización de Alpine.js en el componente `searchable-select` cuando se pulsa "Limpiar filtros", evitando que el texto visible quedara desincronizado del valor Livewire.

---

## 2. Datos de demo — Factories y Seeder

Se crearon factories y un seeder de demo para poblar la base de datos:

- `database/factories/AusenciaFactory.php` — estados ponderados (mayoría aprobadas, algunas pendientes/rechazadas)
- `database/factories/IncidenciaFactory.php` — títulos realistas en español, prioridades y estados variados
- `database/seeders/Fase3DemoSeeder.php` — genera 2-5 ausencias por trabajador y 1-4 incidencias por trabajador (44 ausencias, 31 incidencias en total)

---

## 3. Soft deletes en Ausencias

- Migración `2026_05_28_030911_add_soft_deletes_to_ausencias_table.php` añade `deleted_at` a la tabla `ausencias`
- `app/Models/Ausencia.php` incorpora el trait `SoftDeletes`

---

## 4. Rediseño completo del módulo Ausencias (web)

**Archivos modificados:**
- `app/Livewire/Ausencias/Index.php` — reescritura completa
- `resources/views/livewire/ausencias/index.blade.php` — reescritura completa

**Cambios principales:**
- Estructura idéntica a Conceptos: `x-ui.search-and-filter`, `x-ui.data-table`, `x-ui.modal`, `x-ui.badge`, `x-ui.icon-button`
- Columna **ID** primera en la tabla
- Buscador + panel de filtros dinámico (sin botón "Aplicar", live): Trabajador, Tipo, Estado, Desde, Hasta
- Chips de filtros activos con botón de quitar individual
- Botón **Nuevo** (verde, `+`) para crear ausencias desde la web
- Toggle de **Papelera** con contador
- Acciones: ver / editar / eliminar (con confirmación) / restaurar (en papelera)
- Modal único con modo solo lectura y modo edición; cambio de estado desde el modal de editar
- El loading overlay (`wire:loading.flex` pantalla completa) fue eliminado — no existe en otros módulos
- Filtros `wire:model.live` + métodos `updatedFiltroXxx()` para reset de paginación automático

---

## 5. Exportación Excel + PDF en Ausencias

Implementación completa siguiendo el patrón de Conceptos/Clientes:

| Archivo | Descripción |
|---|---|
| `app/Exports/AusenciasExport.php` | Clase Excel con 12 columnas |
| `app/Http/Controllers/Ausencias/ExportarExcelController.php` | Descarga `.xlsx` con filtros activos |
| `app/Http/Controllers/Ausencias/ExportarPdfController.php` | PDF vertical/horizontal con mPDF |
| `resources/views/pdf/ausencias/lista.blade.php` | Plantilla PDF con logo, colores de empresa y estados coloreados |

- Rutas añadidas: `ausencias.exportar.excel` y `ausencias.exportar.pdf`
- Permiso añadido al seeder: `ausencias.exportar` (ejecutado con `db:seed`)
- Menú "Acciones" en la toolbar con opciones: Exportar a Excel, PDF Vertical, PDF Horizontal
- Listener `x-on:descargar.window` en el blade para descarga invisible

---

## 6. Rediseño completo del módulo Incidencias (web)

**Archivos modificados:**
- `app/Livewire/Incidencias/Index.php` — reescritura completa
- `resources/views/livewire/incidencias/index.blade.php` — reescritura completa

**Cambios principales:**
- Misma estructura que Ausencias/Conceptos
- Columna **ID** primera
- Buscador + panel de filtros dinámico (6 columnas): Trabajador, Tipo, Estado, Prioridad, Desde, Hasta
- Chips de filtros activos con quitar individual
- **Sin botón "Nuevo"** — las incidencias se crean solo desde el móvil
- Acciones: ver / editar / eliminar
- `mount()` usa `Gate::allows()` en lugar de `Auth::user()->can()`
- Loading overlay eliminado
- Ordenación por columna (con soporte especial para `prioridad` usando `FIELD()`)
- Border-left de color según prioridad (rojo=urgente, ámbar=alta) conservado

---

## 7. Modal de editar Incidencias simplificado

El modal de "Gestionar incidencia" fue rediseñado para separar responsabilidades:

- **Bloque gris (siempre solo lectura):** Trabajador, Tipo, Prioridad, Título, Descripción
- **Sección editable:** únicamente Estado + Resolución/Respuesta

`guardar()` en el componente solo valida y actualiza `estado` + `resolucion`. La lógica de `resuelto_por`/`resuelto_at` se preserva si ya estaban asignados (no se sobreescribe al volver a guardar).

---

## 8. Corrección del error TipoIncidencia "otros"

La BD contenía registros con `tipo = 'otros'` que no existe en el enum `TipoIncidencia` (el valor correcto es `'otro'`). Pendiente de corregir los registros en base de datos o actualizar el enum si es un valor válido.

---

## 9. Corrección de firmas en albaranes — Disco de almacenamiento

**Problema:** Las firmas hechas desde el móvil no aparecían en la vista web ni en el PDF descargado.

**Causa raíz:** El componente móvil `Firmar.php` usaba `Storage::put()` (disco privado `storage/app/private/firmas/`) mientras que el PDF y la vista web buscaban las imágenes en `Storage::disk('public')`.

**Fixes aplicados:**

1. `app/Livewire/Mobile/Albaranes/Firmar.php` — `Storage::put()` → `Storage::disk('public')->put()`
2. Migración de los 23 archivos existentes de `storage/app/private/firmas/` a `storage/app/public/firmas/`
3. `resources/views/livewire/albaranes/ver.blade.php` — `$firma->firma_base64` (propiedad inexistente) reemplazado por `Storage::disk('public')->url($firma->firma_path)`; link de descarga corregido igualmente

---

## 10. Select de firmante trabajador en albaranes (web)

**Problema:** El select "quién firma" en la sección de firma del albarán mostraba los trabajadores del **proyecto** asociado, en lugar de los trabajadores que aparecen en las **líneas del albarán**.

**Fix:** `firmantesInternosDisponibles()` en `app/Livewire/Albaranes/Editar.php` ahora recoge los `trabajador_id` de `$this->albaran->lineasPersonal` + el `creado_por`, y carga solo esos usuarios. Si no hay líneas, el select aparece vacío.

---

## 11. Firma desde móvil — albaranes creados desde la web

**Problema:** Cuando un albarán se creaba desde la web y se asignaba un `firma_trabajador_user_id` distinto del creador, ese trabajador no podía acceder a firmar desde el móvil (recibía un 403).

**Causa raíz:** La policy `AlbaranPolicy::firmar` y la propiedad `esTrabajador` del componente móvil solo contemplaban al creador del parte (`creado_por`), ignorando el firmante asignado explícitamente.

**Fixes aplicados:**

1. `app/Policies/AlbaranPolicy.php` — añadido `firma_trabajador_user_id` como firmante válido
2. `app/Livewire/Mobile/Albaranes/Firmar.php`:
   - `esTrabajador` ahora es `true` si el usuario es el creador **o** el `firma_trabajador_user_id`
   - `Gate::authorize()` reemplazado por `Gate::check()` — en lugar de 403 se muestra pantalla amigable
3. `resources/views/livewire/mobile/albaranes/firmar.blade.php`:
   - Nueva pantalla roja "Sin permiso para firmar" con botón de volver (en lugar de error del servidor)
   - El nombre del trabajador en el resumen y en el canvas ahora muestra `firmaTrabajador` si está asignado, o el creador si no lo está

---

## Estado general al cierre de sesión

| Módulo | Estado |
|---|---|
| Ausencias (web) | ✅ Completo — listado, CRUD, papelera, exportación Excel/PDF |
| Incidencias (web) | ✅ Completo — listado, gestión de estado/resolución, filtros |
| Firmas en albaranes | ✅ Corregido — disco público, vista web, PDF, acceso móvil |
| Filtros con códigos | ✅ Aplicado en Horas, Ausencias, Incidencias |
| TipoIncidencia "otros" | ⚠️ Pendiente — registros en BD con valor inválido |
